name: flutter_appli_puppy_clean
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: "^0.13.1"


flutter_launcher_icons:
  android: "launcher_icon"
  windows:
    generate: true
    image_path: "assets/puppy_logo.png"
    icon_size: 48 # min:48, max:256, default: 48
 
flutter:
  uses-material-design: true
  # Pour incorporer tous les éléments du fichier assets
  assets:
  - assets/
  
  # Pour incorporer les polices

  fonts:
      - family: Questrial
        fonts:
            - asset: assets/Questrial-Regular.ttf 

      




